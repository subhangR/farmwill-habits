import 'package:flutter/cupertino.dart';

class UserHabitsDayPage extends StatelessWidget {
  const UserHabitsDayPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('User Habits Day Page'),
    );
  }
}